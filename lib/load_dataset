import os
import numpy as np

def load_st_dataset(dataset):
    #output B, N, D
    #if dataset == '..':
    #".." Please enter the name of your data set
        #data_path = os.path.join('../data/../...npz')
        #Please enter your data set file address here in the code format above.
    else:
        raise ValueError
    if len(data.shape) == 2:
        data = np.expand_dims(data, axis=-1)
    print('Load %s Dataset shaped: ' % dataset, data.shape, data.max(), data.min(), data.mean(), np.median(data))
    return data
